<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a.warehouseassistant">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.flash" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".StartScreen"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login"
            android:label="Logowanie" />
        <activity
            android:name=".Register"
            android:label="Rejestracja" />
        <activity android:name=".UserAreaActivity" />
        <activity
            android:name=".ScannerProduct"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".AddToDB"
            android:label="Dodaj produkty do magazynu" />
        <activity
            android:name=".ScannerShelf"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".ManageDB"
            android:label="Zarządzanie magazynem" />

    </application>

</manifest>